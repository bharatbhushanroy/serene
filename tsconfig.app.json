{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "composite": true,
    "lib": ["DOM", "DOM.Iterable", "ES2020"],
    "module": "ESNext",
    "skipLibCheck": true,
    "jsx": "react-jsx", // Ensure this is also set, inheriting from tsconfig.json
    "types": ["react", "react-dom"]
  },
  "include": ["src"]
}